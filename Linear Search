#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    int A[n];

    // Read array
    for (int i = 0; i < n; i++) {
        cin >> A[i];
    }

    // Read key
    int key;
    cin >> key;

    // Linear Search
    for (int i = 0; i < n; i++) {
        if (A[i] == key) {
            cout << "Element found at position " << i + 1;
            return 0;
        }
    }

    cout << "Element not found";

    return 0;
}
